/* long
comment long
comment */

boolean f2(int a, int d, int y, char c)
{
	char x, b;
	boolean flag;

	b = '&';
	// a = x;  
	// b = 8; 
	a = (y * 7)/a-y;
	// a = (y * 7)/b-y; 
	// a = (y * 7)/a-c; 

	flag = (b == c) && (y > a);
	// flag = (b == c) && (y + a); 
	// 3 + 6 = 9; 
	// ^x = 6; 
	
    	return flag ;
}


int f3(int i, int j, int k)
{
    char x, k;
    boolean f4(int l, int m, int n)
    {
	boolean x, j, n;
	k = '@';
	i = l + l;
	// i = j + 1;
	// i = k + 1; 
	if ((k == '*') || (x != false) && (l+m < i)){
		x = l < m;
	}
	//return m;
        return x;
    }
	boolean k;
    {
    	k=f4(5,i,j);
	// x =f4(5,i,j); 
	// k = f4(5,i); 
	// k = f4(5,x,j); 
     }
    // n = false; 
    x = '#';	
    //return k;
    return 90;
}


int f5(int i, int j, int k)
{
    int f6(int t) /* procedure declarations */ 
    {
        int temp;
        temp = t* t;
        return temp;
    }

    int total;                       /* variable declarations */
    boolean bool;
    bool = f2(i,j,k,'^');
    // j = f1(); 
    total = f6(i+j+k);                    /* statements */
    return 0;
}


int f1() 
{
    string s1[35], s2[70];
    int i, j, tnc1;
    i=0;
    j=0;
    tnc1 = 1;
    while(i<|s1|){
	while(j<|s2|/2){
	    if (s1[i] == s2[j]){
		tnc1 = tnc1*2;
	    }
	    j=j+1;
	}
	i=i+1;
    }
    return tnc1;
}


void f7()
{
	int x;
	intp y;
    {   
	x = 5;
	y = &x;
	x = 6;
	//^x = 7;
	//&x = y;
	//y = x;
	if ((&x == y) && (^y == x)){
		^y = 9;
	}
     	{
		//charp x;
		//string y[10];
		//char z;
		y = "foobar";
		//y='b';
		x = &y[5];
		//x=&y; 
		z = ^x;        
		//z = ^x - 5;       
		y = "barfoo"; 
   		 {
			//char x;
			//intp y;
			//charp z;
			//char g;

			x = ^z; 
			// f = x;
			// x = &(1+3);
			//y = &x;             
			//z = &(&g);
    		  }
		 //z = x;
		 // z = &x;
	}
     }
}



void main()
{
    int b52;
    b52 = f1();
}
